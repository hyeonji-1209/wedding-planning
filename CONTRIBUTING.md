# 기여 방법

공개 레포지만 **작업 방식은 오픈소스식**으로 운영한다 — main 직접 커밋 없음, 전부 PR.

## 흐름

```bash
git switch -c feat/무엇을-되게-만드는지     # 브랜치는 짧게, 하루 안에 머지
# ... 작업 ...
pnpm check
gh pr create                               # 템플릿이 뜬다. Outcome부터 쓴다
```

코어 3인은 브랜치를 이 레포에 직접 만든다. 외부 기여자는 fork 후 PR.

## 규칙

- **main은 항상 동작한다.** 커지는 브랜치는 잘못된 브랜치다. 하루 넘기면 쪼갠다.
- PR 하나 = **Outcome 하나.** 여러 개 섞이면 리뷰가 무의미해진다.
- 구현한 에이전트가 리뷰하지 않는다. **다른 에이전트로 독립 리뷰**한다 (Claude ↔ Codex).
- force push 금지. 3명이 공유하는 히스토리다.
- 제품·구조 결정은 코드가 아니라 `docs/kickoff.md` Decision Log에 남는다.

## 에이전트로 작업할 때

레포가 곧 컨텍스트다. 프롬프트에 배경을 반복해 넣지 말 것:

- `CLAUDE.md` — 매 세션 자동 로드 (명령어·스택·공통 규칙)
- `.claude/rules/*.md` — 해당 영역 파일을 건드릴 때만 자동 로드
- `docs/` — 필요할 때 읽는다. 자동 로드 아님

에이전트에게는 **목표 + 성공조건 + 제약**을 준다. 구현 방법을 지정하지 않는다.
자세한 운영 규칙은 `docs/working-agreement.md`.

## AI 사용 정책

이 팀은 코딩 에이전트를 전제로 일한다. 금지가 아니라 **표시하고 책임지는** 방식이다.

- **PR마다 AI 사용을 밝힌다.** 템플릿의 `AI 사용` 칸. 한 줄이면 된다
- **본인이 설명할 수 있어야 한다.** 리뷰에서 "왜 이렇게 했냐"에 답 못 하면 그 PR은 아직 준비가 안 된 것이다
- **AI로 코드를 쓰는 건 괜찮다. 이해 못 한 채 올리는 건 안 된다**
- **PR 설명은 사람이 손봐서 올린다.** 에이전트에게 초안을 시켜도 된다. 그대로 올리면 diff를 요약한 긴 글이 나오고 리뷰어는 같은 걸 두 번 읽는다. 자기 말로 고치고, 필요 없는 문단은 지우고, 짧게 만든다
- **공시는 실제로 한 대로 쓴다.** 초안을 에이전트가 썼으면 그렇게 적는다. "직접 썼다"고 쓰지 않는다 — 공시란에 거짓말이 들어가면 정책 전체가 장식이 된다
- 커밋 트레일러는 `Assisted-by:` 또는 `Generated-by:`를 쓴다. **`Co-authored-by:`는 AI에 쓰지 않는다** — 공동 저자는 DCO에 서명할 수 있는 사람을 뜻하고 모델은 그럴 수 없다. Linux Kernel·Kubernetes·Mesa·pip 등이 같은 이유로 금지한다

```
Assisted-by: Claude Opus 5
```

## 아직 안 정한 것

**라이선스.** 지금은 공개돼 있을 뿐 오픈소스 라이선스가 없다 = 기본 저작권이 적용되고
제3자에게 사용 권리가 없다. 외부 기여를 받을 생각이면 라이선스를 먼저 정해야 한다.
(`docs/kickoff.md` Q7)
