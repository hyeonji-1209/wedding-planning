# 배포(Vercel) 전용 — 로컬에선 비워두면 미들웨어가 통과시킨다
SITE_PASSWORD=

# 취향 프로필 요약 (없으면 deterministicSummary로 폴백 — summary-claude.ts)
ANTHROPIC_API_KEY=

# 시드 이미지 서빙 + 태깅 파이프라인 (Cut 1: Storage만 사용, Postgres는 Cut 2)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
